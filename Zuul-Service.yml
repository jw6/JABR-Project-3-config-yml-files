server:
  port: 8765

spring:
  application:
    name: Zuul-Service
  datasource:
    tomcat: 
      initial-size: 1

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
hystrix:
  command:
    default:
      execution:
        timeout:
          enabled: false
